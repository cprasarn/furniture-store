<%= form_for @order, :html => { :multipart => true}, :remote => @order.new_record? do |order| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% if @address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@address.errors.count, "error") %> prohibited this adderss from being saved:</h2>

      <ul>
      <% @address.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% if @customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

      <ul>
      <% @customer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="tabs">
    <ul>
      <li><a href="#order_detail">Detail</a></li>
      <li><a href="#order_charge">Price</a></li>
      <li><a href="#order_payment">Payment</a></li>
      <li><a href="#order_note">Note</a></li>
    </ul>

    <div id="order_detail" class="row" style="padding-left:50px">
      <div id="order_info" class="row">
        <%= render 'orders/order_form' %>
      </div>
    
      <div id="customer_detail" class="row">
        <%= render 'customers/order_form' %>
      </div>
      
      <div id="items" class="row" style="margin-top:20px">
        <%= render 'items/order_form' %>
      </div>
    </div>
      
    <div id="order_charge" class="row" style="margin-left:30%">
      <%= render 'orders/price_form' %>
    </div>
    <div id="order_payment" class="row" style="padding-left:60px">
      <%= render 'ledgers/order_form' %>
    </div>
    <div id="order_note" class="row" style="padding-left:60px">
      <%= render 'notes/order_form' %>
    </div>
  </div>
  <div class="row"></div>
  <div class="row" style="padding-top:20px;">
    <div class="col_r">
      <%= button_tag "Submit",
        :id => "submit_button",
        :name => "submit_button",
        :type => "submit" 
      %>
    </div>
  </div>
<% end %>
  